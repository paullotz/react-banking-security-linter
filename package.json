{
	"name": "react-banking-security-linter-monorepo",
	"private": true,
	"engines": {
		"node": ">=18",
		"pnpm": ">=8"
	},
	"scripts": {
		"build": "pnpm -r run build",
		"test": "vitest run",
		"test:watch": "vitest",
		"lint": "pnpm -r run lint"
	},
	"devDependencies": {
		"@types/node": "^20.0.0",
		"eslint-plugin-react-banking-security": "workspace:*",
		"typescript": "^5.4.0",
		"vitest": "^1.4.0"
	}
}
